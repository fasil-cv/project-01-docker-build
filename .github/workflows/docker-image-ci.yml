name: Docker CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  APP_NAME: myapp

jobs:
  # Build the image
  build-image:
    runs-on: ubuntu-latest  
    steps:
      - uses: actions/checkout@v4

      - name: build
        run: docker build -t ${{ env.APP_NAME }}:local .
      - name: save image
        run: docker save -o ${{ env.APP_NAME }}.tar ${{ env.APP_NAME }}:local

      - uses: actions/upload-artifact@v4
        with:
          name: docker-image
          path: ${{ env.APP_NAME }}.tar
  
  # Test jobs
  test-image:
    needs: build-image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: docker-image
      - name: Load docker image
        run: docker load -i ${{ env.APP_NAME }}.tar

      - name: Run docker image
        run: |
          docker run -d --name=${{ env.APP_NAME }} -p 8080:80 ${{ env.APP_NAME }}:local
          sleep 3
      
      - name: Health check
        run: |
          curl -f http://localhost:8080/healthz
  
  # Security scan image    
  scan-image:
    needs: build-image
    runs-on: ubuntu-latest
    steps:
    
      - uses: actions/download-artifact@v4
        with:
          name: docker-image
      - name: Load docker image
        run: docker load -i ${{ env.APP_NAME }}.tar

      - name: Run Trivy scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ env.APP_NAME }}:local'
          format: 'table'
          exit-code: '0'   #make 1 exiting code to fail the pipeline, if one of the are reported.
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH,MEDIUM,LOW'

  test3:
    needs: test-image
    runs-on: ubuntu-latest
    steps:
      - run: echo "test-3"

